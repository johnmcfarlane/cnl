#!/usr/bin/env bash

# specialised alternative to init-build tailored to GCC toolchain

set -euo pipefail

PROJECT_DIR=$(
  cd "$(dirname "$0")"/../..
  pwd
)

"${PROJECT_DIR}/test/scripts/init-build" \
  --profile "${PROJECT_DIR}/.github/conan/profiles/linux-gcc" \
  --settings compiler.version=10 \
  "$@"
